<!-- views/partials/search/user_results.ejs -->

<table>
  <% for(var i=0; i < results.length; i++) { %>
    <tr href=/users?id=<%= results[i]._id %> >
      <td>
        <img src=<%= results[i].picture %> alt="listing-image">
      </td>
      <td><%= results[i].username %></td>
      <td><%= results[i].description %></td>
      <td><%= results[i]._id %></td>
    </tr>
  <% } %>
</table>